import MessagesPage from "../student/MessagesPage";

const ExpertMessagesPage = () => {
  // Same UI & logic, just accessed from expert dashboard
  return <MessagesPage />;
};

export default ExpertMessagesPage;
